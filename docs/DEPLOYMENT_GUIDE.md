# Руководство по сборке и развертыванию

## 📋 Варианты использования

### 1. Разработка и тестирование (Windows)

**Что нужно:**
- Node.js 20+
- npm

**Как работает:**
```bash
cd proxy-server
npm install          # Установить зависимости (один раз)
npm run start:dev    # Запуск через tsx (без сборки)
```

**Что происходит:**
- `tsx` запускает TypeScript напрямую, без компиляции
- Изменения в коде применяются сразу (перезапуск вручную)
- Используется для локальной разработки и тестирования

**Файлы, которые используются:**
- `src/index.ts` - исходный код
- `package.json` - зависимости
- `tsconfig.json` - настройки TypeScript
- `node_modules/` - установленные зависимости

---

### 2. Развертывание БЕЗ Docker (Ubuntu/любой Linux)

**Что нужно на сервере:**
- Node.js 20+
- npm

**Что копировать на сервер:**
```
proxy-server/
  ├── src/              ✅ НУЖНО (исходный код)
  ├── package.json      ✅ НУЖНО (зависимости)
  ├── package-lock.json ✅ НУЖНО (точные версии)
  ├── tsconfig.json     ✅ НУЖНО (настройки сборки)
  └── README.md         ⚪ Опционально (документация)
```

**Что НЕ нужно копировать:**
- `node_modules/` - установится на сервере
- `dist/` - соберется на сервере
- `.git/` - не нужен для работы
- `.vscode/`, `.idea/` - настройки IDE

**Как развернуть:**
```bash
# 1. Скопировать папку proxy-server/ на сервер (через scp, git, или вручную)
# 2. На сервере:
cd proxy-server
npm install          # Установить зависимости
npm run build        # Собрать TypeScript → JavaScript
npm start            # Запустить (использует dist/index.js)
```

**Что происходит:**
- `npm install` - устанавливает зависимости из `package.json`
- `npm run build` - компилирует `src/index.ts` → `dist/index.js`
- `npm start` - запускает `node dist/index.js`

---

### 3. Развертывание С Docker (Ubuntu/любой Linux) ⭐ РЕКОМЕНДУЕТСЯ

**Что нужно на сервере:**
- Docker (устанавливается одной командой)

**Что копировать на сервер:**
```
proxy-server/
  ├── src/              ✅ НУЖНО (исходный код)
  ├── package.json      ✅ НУЖНО (зависимости)
  ├── package-lock.json ✅ НУЖНО (точные версии)
  ├── tsconfig.json     ✅ НУЖНО (настройки сборки)
  ├── Dockerfile         ✅ НУЖНО (инструкции для Docker)
  ├── .dockerignore     ✅ НУЖНО (что исключить из образа)
  └── docker-compose.yml ⚪ Опционально (удобство)
```

**Что НЕ нужно копировать:**
- `node_modules/` - установится внутри Docker образа
- `dist/` - соберется внутри Docker образа
- `.git/` - не нужен для работы

**Как развернуть:**
```bash
# 1. Скопировать папку proxy-server/ на сервер
# 2. На сервере:
cd proxy-server
docker build -t sami-llm-proxy .    # Собрать образ (один раз)
docker run -d \
  --name sami-llm-proxy \
  -p 8080:8080 \
  --env-file .env \
  --restart unless-stopped \
  sami-llm-proxy                     # Запустить контейнер
```

**Что происходит:**
1. `docker build` - создает Docker образ:
   - Копирует файлы в образ
   - Устанавливает зависимости (`npm ci`)
   - Собирает TypeScript (`npm run build`)
   - Готовый образ содержит все необходимое
2. `docker run` - запускает контейнер из образа
   - Контейнер изолирован от системы
   - Работает одинаково везде

---

## 🔄 Можно ли собрать Docker образ на Windows и использовать на Ubuntu?

**ДА! ✅** Docker образы кроссплатформенные.

**Как это работает:**
```bash
# На Windows:
cd proxy-server
docker build -t sami-llm-proxy .           # Собрать образ
docker save sami-llm-proxy > proxy.tar     # Сохранить образ в файл

# Перенести proxy.tar на Ubuntu (через scp, usb, и т.д.)

# На Ubuntu:
docker load < proxy.tar                    # Загрузить образ
docker run -d --name sami-llm-proxy -p 8080:8080 sami-llm-proxy
```

**Преимущества:**
- ✅ Сборка один раз на Windows
- ✅ На Ubuntu только запуск (быстро)
- ✅ Не нужен Node.js на Ubuntu
- ✅ Не нужна сборка на Ubuntu

**Но есть нюанс:**
- Docker образы для Linux собираются на Linux (или через WSL2 на Windows)
- Если у вас Windows без WSL2, лучше собирать на Ubuntu напрямую

---

## 📦 Минимальный набор файлов для копирования

**Для развертывания БЕЗ Docker:**
```
proxy-server/
  ├── src/
  ├── package.json
  ├── package-lock.json
  └── tsconfig.json
```

**Для развертывания С Docker:**
```
proxy-server/
  ├── src/
  ├── package.json
  ├── package-lock.json
  ├── tsconfig.json
  ├── Dockerfile
  └── .dockerignore
```

**Можно скопировать всю папку** (кроме `node_modules/` и `dist/`) - это проще и безопаснее.

---

## 🔐 Настройка аутентификации (логин/пароль)

Прокси-сервер поддерживает Basic Auth через переменные окружения.

### Вариант 1: Через `docker run` (простой способ)

```bash
docker run -d \
  --name sami-llm-proxy \
  -p 8080:8080 \
  -e PROXY_AUTH_USERNAME=admin \
  -e PROXY_AUTH_PASSWORD=your-secure-password \
  --restart unless-stopped \
  sami-llm-proxy
```

**⚠️ Внимание:** Пароль виден в истории команд. Для production используйте другие способы.

---

### Вариант 2: Через `docker-compose.yml` (удобно)

Отредактируйте `docker-compose.yml`:

```yaml
services:
  sami-llm-proxy:
    build: .
    container_name: sami-llm-proxy
    ports:
      - "8080:8080"
    environment:
      - PROXY_PORT=8080
      - PROXY_AUTH_USERNAME=admin          # Раскомментируйте
      - PROXY_AUTH_PASSWORD=your-password  # Раскомментируйте
      - LOG_LEVEL=info
    restart: unless-stopped
```

Затем:
```bash
docker-compose up -d
```

---

### Вариант 3: Через `.env` файл (безопаснее)

**Создайте файл `.env` в папке `proxy-server/`:**

```bash
PROXY_AUTH_USERNAME=admin
PROXY_AUTH_PASSWORD=your-secure-password
PROXY_PORT=8080
LOG_LEVEL=info
```

**Важно:** Добавьте `.env` в `.gitignore`, чтобы не коммитить пароли!

**Использование с docker-compose:**

Обновите `docker-compose.yml`:
```yaml
services:
  sami-llm-proxy:
    build: .
    container_name: sami-llm-proxy
    ports:
      - "8080:8080"
    env_file:
      - .env  # Загружает переменные из .env файла
    restart: unless-stopped
```

Затем:
```bash
docker-compose up -d
```

**Использование с docker run:**

```bash
docker run -d \
  --name sami-llm-proxy \
  -p 8080:8080 \
  --env-file .env \
  --restart unless-stopped \
  sami-llm-proxy
```

---

### Вариант 4: Через системные переменные окружения (самый безопасный)

**На Ubuntu:**

```bash
# Установить переменные в системе (в ~/.bashrc или /etc/environment)
export PROXY_AUTH_USERNAME=admin
export PROXY_AUTH_PASSWORD=your-secure-password

# Или создать файл /etc/sami-proxy.env
sudo nano /etc/sami-proxy.env
# Добавить:
# PROXY_AUTH_USERNAME=admin
# PROXY_AUTH_PASSWORD=your-secure-password

# Использовать:
docker run -d \
  --name sami-llm-proxy \
  -p 8080:8080 \
  --env-file /etc/sami-proxy.env \
  --restart unless-stopped \
  sami-llm-proxy
```

---

### Как это работает

1. **Если `PROXY_AUTH_USERNAME` и `PROXY_AUTH_PASSWORD` заданы:**
   - Прокси требует аутентификацию
   - Клиент должен отправлять заголовок `Proxy-Authorization: Basic <base64>`
   - Формат: `Basic base64(username:password)`

2. **Если не заданы:**
   - Прокси работает без аутентификации (открытый доступ)

3. **В настройках Sami:**
   - Укажите `Username` и `Password` в настройках прокси
   - Приложение автоматически добавит заголовок аутентификации

---

### Примеры использования

**Без аутентификации:**
```bash
docker run -d --name sami-llm-proxy -p 8080:8080 sami-llm-proxy
```

**С аутентификацией:**
```bash
docker run -d \
  --name sami-llm-proxy \
  -p 8080:8080 \
  -e PROXY_AUTH_USERNAME=admin \
  -e PROXY_AUTH_PASSWORD=secret123 \
  sami-llm-proxy
```

**Проверка работы:**
```bash
# Без аутентификации
curl -x http://localhost:8080 https://api.openai.com/v1/models

# С аутентификацией
curl -x http://admin:secret123@localhost:8080 https://api.openai.com/v1/models
```

---

## 🎯 Рекомендации

### Для разработки на Windows:
```bash
npm run start:dev    # Быстро, без сборки
```

### Для развертывания на Ubuntu:
```bash
# Вариант 1: Docker (лучше) ⭐
docker build -t sami-llm-proxy .
docker run -d --name sami-llm-proxy -p 8080:8080 --restart unless-stopped sami-llm-proxy

# Вариант 2: Без Docker
npm install
npm run build
npm start
```

**Почему Docker лучше:**
- ✅ Не нужен Node.js на сервере
- ✅ Изолирован от системы
- ✅ Легко обновлять
- ✅ Работает одинаково везде

---

## 🔍 Что происходит при сборке

### Без Docker:
1. `npm install` → устанавливает зависимости в `node_modules/`
2. `npm run build` → компилирует `src/index.ts` → `dist/index.js`
3. `npm start` → запускает `node dist/index.js`

### С Docker:
1. `docker build` → внутри образа:
   - Копирует файлы
   - `npm ci` → устанавливает зависимости
   - `npm run build` → компилирует TypeScript
   - Создает готовый образ
2. `docker run` → запускает контейнер из образа

---

## 📝 Итоговая таблица

| Вариант | Где | Что нужно | Что копировать | Команды |
|---------|-----|----------|----------------|---------|
| **Разработка** | Windows | Node.js | Вся папка | `npm install`<br>`npm run start:dev` |
| **Без Docker** | Ubuntu | Node.js | `src/`, `package*.json`, `tsconfig.json` | `npm install`<br>`npm run build`<br>`npm start` |
| **С Docker** | Ubuntu | Docker | `src/`, `package*.json`, `tsconfig.json`, `Dockerfile` | `docker build`<br>`docker run` |

